# Wersje dobrane pod Python 3.14 (koła cp314 / py3-none-any).
# Paczki z kodem natywnym (pydantic-core, cryptography) wymagają wersji
# z gotowymi wheelami dla 3.14 — stąd >= zamiast sztywnego pinu z promptu (3.12).

fastapi>=0.115.0
uvicorn[standard]>=0.30.1
# supabase przypięty: nowsze wersje ciągną storage3 → pyiceberg (brak koła cp314,
# wymaga MSVC). 2.5.0 ma czyste zależności i działa z pydantic 2.x.
supabase==2.5.0
celery>=5.4.0
redis>=5.0.4
weasyprint>=62.3
python-jose[cryptography]>=3.3.0
pydantic>=2.11.0
pydantic-settings>=2.3.4
jinja2>=3.1.4
httpx>=0.27.0
python-multipart>=0.0.9
resend>=2.0.0

# dev / test
pytest>=8.2.2
pytest-asyncio>=0.23.7
